# Your Linux host IP address — this gets baked into implant.exe at build time
# so the implant automatically connects back to this IP when run on the Windows VM.
#
# Find your IP with:
#   ip addr show | grep "inet " | grep -v 127.0.0.1
#
C2_HOST_IP=
