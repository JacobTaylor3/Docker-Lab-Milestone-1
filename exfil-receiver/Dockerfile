FROM node:20-slim

WORKDIR /opt/exfil-receiver

COPY exfil-receiver/server.js ./

# nginx certs generated by launch.sh — same cert reused for exfil HTTPS
COPY certs/nginx.crt /etc/certs/nginx.crt
COPY certs/nginx.key /etc/certs/nginx.key

EXPOSE 9443

CMD ["node", "server.js"]
